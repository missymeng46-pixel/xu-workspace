# 安全说明

## 支持范围

序 XU 当前定位为本地单用户工作台。它可以在可信局域网内提供手机访问，但没有为公网部署设计完整的多用户认证、权限隔离和抗攻击能力。

## 请勿公开的文件

- `.env`
- `data/` 及其中的 SQLite 数据库
- 运行日志
- 手机临时访问码
- 包含个人财务、任务或客户信息的截图

这些内容已包含在 `.gitignore` 中，但提交前仍应检查 `git status`。

## 部署建议

- 默认只在自己的电脑或可信家庭网络使用。
- 不要进行路由器端口映射。
- 不要直接将 `4173` 端口暴露到公网。
- 公网部署前需要增加 HTTPS、正式用户认证、CSRF 防护、速率限制和多租户数据隔离。
- DeepSeek API Key 只保存在 `.env`，不要写进前端文件。

## 报告安全问题

请通过 GitHub 仓库的安全报告渠道联系维护者，不要在公开 Issue 中粘贴密钥、数据库或个人数据。

